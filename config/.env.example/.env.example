# Example Environment Variables for Personal Finance Tracker Application

# Username for the application (change as per installation process)
APP_USERNAME=your_username_here

# Password for the application (change as per installation process)
APP_PASSWORD=your_password_here

# Filename for transaction data storage
TRANSACTION_FILE=transactions.json

# Filename for authentication data storage
AUTH_FILE=auth.json

# Application environment (development/production)
ENVIRONMENT=development

# Debug mode setting (set to 'True' for development, 'False' for production)
DEBUG=True
